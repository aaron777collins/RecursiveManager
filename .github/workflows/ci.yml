# ⚠️ CI DISABLED - This project is no longer under active development
# See README.md for details

name: CI

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  disabled:
    name: CI Disabled
    runs-on: ubuntu-latest
    if: false  # Never run
    steps:
      - run: echo "CI is disabled for this repository"
