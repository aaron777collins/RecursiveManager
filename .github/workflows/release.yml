# ⚠️ RELEASE WORKFLOW DISABLED - This project is no longer under active development
# See README.md for details

name: Release

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  disabled:
    name: Release Disabled
    runs-on: ubuntu-latest
    if: false  # Never run
    steps:
      - run: echo "Release workflow is disabled for this repository"
