# ⚠️ DOCKER SECURITY SCAN DISABLED - This project is no longer under active development
# See README.md for details

name: Docker Security Scan

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  disabled:
    name: Docker Security Scan Disabled
    runs-on: ubuntu-latest
    if: false  # Never run
    steps:
      - run: echo "Docker security scan is disabled for this repository"
