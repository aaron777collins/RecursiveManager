# ⚠️ DOCS DEPLOYMENT DISABLED - This project is no longer under active development
# See README.md for details

name: Deploy Documentation

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  disabled:
    name: Docs Deployment Disabled
    runs-on: ubuntu-latest
    if: false  # Never run
    steps:
      - run: echo "Documentation deployment is disabled for this repository"
